- [solved]   - bug00 - when adding a new contact 1 bytes of sram randomly leaks (was the contact list not being freed after being populated in get_all_contacts function)
- [solved]   - bug01 - after some time the device just crashes out of nothing, it was due to a false interrupt request generated by the interrupt pin of the sx1278 module, that was catching some weird noise, so the payload is null and everything breaks apart
- [solved]   - bug02 - malloc can't spell your name properly
- [solved]   - bug03 - joystick east and west are flipped all around the code (just swapped E and W)
- [solved]   - bug04 - when messaging a contact 1 bytes of sram randomly leaks (was the transaction_uid of the attempt_single_transaction function not being freed)
- [solved]   - bug05 - when messages clearly arrives to the recipient device the sending one keeps telling they were not received, and vice-versa
- [solved]   - bug06 - loading a non 10-length chunk of a conversation will make the system crash (chunk index was not properly parsed since its memory was freed before)
- [solved]   - bug07 - messages are not correctly delivered if receiving user is in one of the menu (is interacting with the device outside of home screen)
- [solved]   - bug08 - graphics glitches during tutorial
- [solved]   - bug09 - joystick W and E are flipped in tutorial
- [solved]   - bug10 - if a message arrives when the recipient is displaying the notification screen from the previously arrived message, the new message will not get delivered
- [solved]   - bug11 - memory leak when password requests to access terminal returns false (wrong password)
- [solved]   - bug12 - messages from unknown address are not properly saved
- [solved]   - bug13 - when scanning for online contacts no device can be found even if they are on and in range (the irq_flags reg of the sx1278 was not properly cleared)
- [solved]   - bug14 - when setting the alarm, modifying the last seconds digit makes the system crash (at_change_callback of "set_hours_tens" was set to NULL)
- [solved]   - bug15 - memory leak when setting alarm (it is not a leak, the alarm message is stored in a static char)
- [solved]   - bug16 - alarm does not fires if clock speed is reduced during standby (looks like it does works)
- [solved]   - bug17 - after sx1278 goes to sleep it does not work when set to rx mode (sx1278 needs to be re-initialized)
- [solved]   - bug18 - passwords longer than 15 characters breaks password manager (was the sdcard buffer size in sdcard_write_file that was splitting the same line across multiple lines)
- [solved]   - bug19 - when inserting a newline character in text editor it will sometime delete a character and put an empty space instead of it, always happening when a word is wrapped between two lines (it needed a condition on the logical row)
